# Environment Variables for Dragons GraphQL API

# Optional: Space-separated list of valid API tokens for authentication
# If not set, the API will be publicly accessible
# API_USERS="token1 token2 token3"

# Optional: Apollo Studio API key for monitoring
# ENGINE_API_KEY=your_apollo_studio_api_key
